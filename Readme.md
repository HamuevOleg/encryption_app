
# 🔐 Encryption Playground

**Encryption Playground** — это интерактивное веб-приложение для демонстрации и изучения современных алгоритмов шифрования. Приложение позволяет пользователям генерировать ключи, шифровать и расшифровывать текст в реальном времени, визуализируя процесс.

## 🛠 Технологический стек

Проект реализован в архитектуре монорепозитория, разделенного на Frontend и Backend.

### Frontend (Клиентская часть)

* **Фреймворк:** [Angular 17+](https://angular.io/) (использует Standalone Components).
* **Управление состоянием:** Angular Signals (`signal`, `computed`) для реактивности.
* **Стилизация:** SCSS с использованием CSS-переменных для темы "Cyber Dark".
* **Сборщик:** Angular CLI / Vite (через Angular build system).

### Backend (Серверная часть)

* **Runtime:** [Bun](https://bun.sh/) — современный и быстрый JavaScript runtime.
* **Фреймворк:** [ElysiaJS](https://elysiajs.com/) — высокопроизводительный веб-фреймворк для Bun.
* **Документация API:** Swagger (автоматически генерируемая документация).
* **Криптография:** Встроенные модули Node/Bun crypto и библиотеки `elliptic`, `bn.js`.

### Инфраструктура

* **Docker & Docker Compose:** Для контейнеризации и одновременного запуска всех сервисов.

-----

## 🚀 Как запустить проект

Самый простой способ запустить приложение — использовать Docker Compose.

### Вариант 1: Через Docker (Рекомендуется)

Убедитесь, что у вас установлен Docker и Docker Compose.

1.  В корне проекта выполните команду:
    ```bash
    docker-compose up --build
    ```
2.  Откройте браузер:
    * **Frontend:** `http://localhost:4200` (или `http://localhost:80`, в зависимости от настройки nginx/порта в docker-compose).
    * **Backend API:** `http://localhost:3000`
    * **Swagger Docs:** `http://localhost:3000/swagger`

### Вариант 2: Ручной запуск (для разработки)

**Backend:**

1.  Перейдите в папку backend: `cd backend`
2.  Установите зависимости: `bun install`
3.  Запустите сервер: `bun dev`

**Frontend:**

1.  Перейдите в папку frontend: `cd frontend`
2.  Установите зависимости: `npm install`
3.  Запустите приложение: `npm start` (или `ng serve`)

-----

## 🛡 Реализованные методы шифрования

Приложение поддерживает три основных алгоритма, охватывающих симметричное и асимметричное шифрование:

1.  **AES (Advanced Encryption Standard)**

    * **Тип:** Симметричное шифрование (один ключ для шифрования и дешифрования).
    * **Поддерживаемые размеры ключа:** 128, 192, 256 бит.
    * **Особенности в приложении:** Позволяет генерировать секретный ключ, шифровать текст блоками. Визуализация показывает этапы (SubBytes, ShiftRows и т.д.).

2.  **RSA (Rivest–Shamir–Adleman)**

    * **Тип:** Асимметричное шифрование.
    * **Пара ключей:** Публичный (для шифрования) и Приватный (для дешифрования).
    * **Особенности:** Используется для безопасной передачи данных без предварительного обмена секретом. В приложении можно сгенерировать пару ключей и скачать их в формате PEM.

3.  **ECC (Elliptic Curve Cryptography)**

    * **Тип:** Асимметричное шифрование на эллиптических кривых.
    * **Особенности:** Обеспечивает ту же стойкость, что и RSA, но с гораздо меньшим размером ключа (например, 256-битный ключ ECC сравним с 3072-битным ключом RSA). Более эффективен и быстр.

-----

## 🏗 Архитектура и Принцип работы

### Как работает Frontend

Клиентская часть — это SPA (Single Page Application) на Angular.

1.  **Компоненты:** Основная логика сосредоточена в `App` компоненте. Он управляет выбором алгоритма, вводом текста и отображением результатов.
2.  **Signals:** Вместо традиционных `Zone.js` или `RxJS` (для состояния), приложение активно использует **Signals** (`readonly plaintext = signal('')`, `computed(...)`) для мгновенного обновления UI при изменении данных.
3.  **Визуализация:**
    * Конвертация текста в бинарный вид для наглядности.
    * Анимация потока данных (CSS-анимации в `app.scss`) для демонстрации процесса шифрования.
4.  **История:** Использует `localStorage` браузера для сохранения истории операций (шифрование/дешифрование) без отправки этих данных в базу данных.
5.  **Взаимодействие:** Через `EncryptionApiService` отправляет HTTP-запросы на Backend для выполнения тяжелых криптографических операций.

### Как работает Backend

Серверная часть построена на **ElysiaJS** (на базе Bun), что обеспечивает высокую скорость обработки запросов.

1.  **Роутинг:** Elysia обрабатывает входящие REST-запросы.
2.  **Сервис шифрования:** Файл `encryption.service.ts` содержит бизнес-логику. Он использует криптографические примитивы для выполнения операций.
3.  **Валидация:** Использование DTO (Data Transfer Objects) для проверки входных данных перед обработкой.
4.  **CORS:** Настроен для приема запросов с фронтенда.

-----

## 📡 API Endpoints (Запросы)

Backend предоставляет следующий API для взаимодействия:

### 🔑 Управление ключами

| Метод | Путь | Параметры | Описание |
| :--- | :--- | :--- | :--- |
| `GET` | `/keys/aes` | `?size=128/192/256` | Генерирует случайный секретный ключ AES указанной длины. |
| `GET` | `/keys/rsa` | - | Генерирует пару ключей RSA (Public/Private) в формате PEM. |
| `GET` | `/keys/ecc` | - | Генерирует пару ключей ECC (на кривой secp256k1 или prime256v1). |

### 🔒 Криптографические операции

| Метод | Путь | Тело запроса (JSON) | Описание |
| :--- | :--- | :--- | :--- |
| `POST` | `/encrypt` | `{ method: 'AES'\|'RSA'\|'ECC', text: '...', key/publicKey: '...' }` | Шифрует переданный текст выбранным алгоритмом с использованием ключа. Возвращает зашифрованный текст (обычно Base64 или Hex). |
| `POST` | `/decrypt` | `{ method: 'AES'\|'RSA'\|'ECC', encryptedText: '...', key/privateKey: '...' }` | Расшифровывает данные обратно в исходный текст. |

-----

## 📂 Структура проекта

```text
encryption_app/
├── backend/              # Серверная часть (Bun + Elysia)
│   ├── src/
│   │   ├── encryption.service.ts  # Логика шифрования
│   │   └── index.ts               # Точка входа API
│   ├── Dockerfile
│   └── package.json
│
├── frontend/             # Клиентская часть (Angular)
│   ├── src/app/
│   │   ├── services/     # API сервис
│   │   ├── models/       # Интерфейсы TS
│   │   ├── app.ts        # Логика компонента
│   │   ├── app.html      # Верстка
│   │   └── app.scss      # Стили
│   ├── package.json
│   └── angular.json
│
└── docker-compose.yml    # Оркестрация контейнеров
```